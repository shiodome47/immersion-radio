{
  "name": "immerse-fm",
  "version": "0.1.0",
  "private": true,
  "description": "Immerse FM — a personalized i+1 immersion radio for language learners.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
